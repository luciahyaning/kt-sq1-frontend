import { ChangeEvent } from "react";

//type for onChangeListener
type onChangeListener = (e: ChangeEvent<HTMLInputElement>) => void;

//type for onClickListener
type onClickListener = (e: MouseEvent<HTMLButtonElement>) => void;
